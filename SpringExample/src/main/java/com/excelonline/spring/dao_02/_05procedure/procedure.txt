CREATE OR REPLACE PROCEDURE GET_EMP_DATA
	(NAME IN VARCHAR,
         NO OUT NUMBER,   
	 DESG OUT VARCHAR,
         SALARY OUT NUMBER)
AS

BEGIN
       SELECT EMPNO, JOB, SAL
             INTO NO, DESG, SALARY 
             FROM EMP
             WHERE ENAME = NAME;
END;
/
